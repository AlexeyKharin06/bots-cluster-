# cycle 20260521_0000 — regime head-fake confirmed + BSC universe blind-spot exposed + H_BSC_BC_FULL strong descriptive signal

## Context entering cycle

- BRIEF posture from cycle_1800: H_REGIME_GUARD both conditions clear simultaneously (first time since 05-18 collapse); H_V3 borderline-fail on TEST (Er=-0.031, K=0); next-cycle plan called for re-running H_V3 on **first-fire pnl** (production-realistic).
- Brain context snapshot: `closed=4983 last100 avgPnL=-42.2% WR=29% rug=51%`. closed grew 4888→4983 (+95 trades) since cycle_1800.
- Cycle picked **Task A** (regime delta + planned H_V3 first-fire re-test) — but **the analysis pivoted hard mid-cycle** when probe.js exposed a major blind spot: **332 BSC trades (~7% of dataset) the brain has never analyzed**, containing the largest cluster of fat tails (6 bigs, 1 huge) in any window since cycle_1639.

## Step 1 — regime delta (regime.js)

| metric | cycle_1800 | cycle_0000 | delta |
|---|---|---|---|
| total closed_trades | 4888 | 4983 | +95 |
| Solana rows | 4627 | 4651 (at start; **dropped to 4475** by mid-cycle as state.json rotated live) | trimming faster than appending |
| Unique Sol tokens (first-fire) | 561 | 570 (later 549) | rotating heavily |
| **Big winners Sol (first-fire ≥150%)** | 3 (PIGEON, MTFR, RONALDO) | **1** (RONALDO only) | **PIGEON+MTFR rotated out** |
| Big winners Sol (best-fire) | 4 (with Together) | 2 (Together+RONALDO) | -2 (PIGEON+MTFR rotated) |
| Huge (≥1000%) Sol | 1 (PIGEON) | **0** | PIGEON gone |
| last50 avg | -43.8 | **-56.6** | **-12.8pt regression** |
| last50 WR | 22 | 22 | flat |
| last50 rug | 40 | 56 | +16 |
| **last50 big%** | **2.00%** (RONALDO) | **0** | re-zero (RONALDO rotated out of last50 window) |

**Regime guard status — BOTH CONDITIONS RE-TRIGGERED**:
- Condition A (rolling-50 avg < -55%): **RE-TRIGGERED.** -43.8 → -56.6 (-12.8pt). Cycle_1800's caveat "head-fake risk if re-trigger in 1-2 cycles" — fired in exactly 1 cycle.
- Condition B (big%=0 in last 50): **RE-TRIGGERED.** RONALDO @ 15:41Z slid out of last50 window (now idx 520-570 = 19:27→23:37, post-RONALDO).

**Honest reading**: cycle_1800's "both clear simultaneously" was a **single-cycle head-fake**, identical pattern to cycle_1200→1328. The regime has not recovered.

Sliding-50 last 7 windows (step 25):
```
idx 370-420 2026-05-20T07:28..2026-05-20T10:46 avg=-42.7 WR=28 rug=42 big=0.00
idx 395-445 2026-05-20T09:20..2026-05-20T12:57 avg=-53.4 WR=20 rug=52 big=0.00
idx 420-470 2026-05-20T10:49..2026-05-20T14:50 avg=-58.6 WR=20 rug=56 big=0.00
idx 445-495 2026-05-20T13:00..2026-05-20T17:43 avg=-44.0 WR=22 rug=40 big=2.00  ← RONALDO captured
idx 470-520 2026-05-20T14:58..2026-05-20T19:23 avg=-47.5 WR=18 rug=42 big=2.00
idx 495-545 2026-05-20T17:43..2026-05-20T21:02 avg=-55.0 WR=24 rug=54 big=0.00
idx 520-570 2026-05-20T19:27..2026-05-20T23:37 avg=-56.6 WR=22 rug=56 big=0.00  ← current
```

The recovery peaked at -44.0 (idx 445-495) when RONALDO landed; afterwards drifted back to -56.6. This shape is structurally identical to cycle_1200→cycle_1328 (peak -36.5 → -56.2). **Each regime "recovery" is a 1-2-window local maximum, not a transition.**

## Step 2 — state.json rotation reality check

I ran the same trade-count query twice (regime.js, then later check_evm.js). Total trades: **4983 → 4807** within ~10 minutes of analysis. State.json is being **actively rotated** as new trades enter — old trades evicted to maintain ~5000-row cap.

**Consequence**: cycle_1800's PIGEON +3699 and MTFR +251 are **already gone**. The symbol "PIGEON" now appears in current data only as different tokens (e.g., GxUZUkW8... @ 2026-05-20T18:33Z pnl=20.3). Memecoins re-use symbols liberally; the brain MUST identify big winners by `token` mint address, never by symbol.

The dataset is a **rolling ~3-day window**, not a growing archive. Brain memory (HISTORY.md, insights/) is the only durable record of bigs older than ~3 days. Walk-forward backtests must accept that TRAIN data shifts each cycle as old tokens roll out.

## Step 3 — H_V3 first-fire re-test (the planned task) — DEPRIORITIZED

Planned task: re-run H_BIG_WINNER_SHAPE_V3 walk-forward on first-fire pnl (cycle_1800 deferred this as "production-realistic" check).

**Result before running**: Sol first-fire bigs = 1 (RONALDO only). Sol best-fire bigs = 2 (Together + RONALDO). With only 1-2 bigs in the entire universe, walk-forward on H_V3 is **uninformative** — TEST would have 0-1 bigs and we'd be making n=20 vs n=1 calls.

**Decision**: skip the H_V3 first-fire re-test this cycle. The PIGEON+MTFR rotation removed the statistical basis. Re-test will only be meaningful when 2+ new Sol bigs enter the dataset.

## Step 4 — schema check ⇒ discovery: BSC universe blind-spot

Probe.js revealed: `closed_trades` has `chain` field, and 332 of 4807 rows are `chain=bsc`. Previous cycles (1639 onwards) used the filter `token.length > 20 && !token.startsWith('0x')` — which correctly excludes EVM addresses but **silently excluded an entire trading universe the brain never analyzed**.

This is **not a bug in any prior cycle's code** — it's a methodological blind spot. The filter was the right one for "Solana-only" research, but no cycle ever asked "what's outside the Solana set?". The blind spot has been carried for ~10 cycles.

**Discovery cost**: 1 schema-probe (`Object.keys(evm[0])`).

## Step 5 — BSC universe profile

### Counts & aggregate
- BSC trades: 332 rows = 97 unique tokens (avg 3.4 rows/token, much less than Sol's ~8x).
- Solana trades: 4475 rows = 549 unique tokens (with chain-field filter applied; consistent with prior cycles).
- BSC span: 2026-05-18T13:00Z → 2026-05-20T23:23Z (2.43 days).
- BSC dexes: pancakeswap (330), v2 (2).
- BSC chains: bsc only.

### Per-stream stats on BSC universe (per-stream first-fire dedup)
| stream | n | avg | WR | rug | big% | huge% |
|---|---|---|---|---|---|---|
| **SNIPER_B** | **97** | **-7.2** | **25** | **57** | **6.19** | **1.03** |
| SNIPER_A | 97 | -62.8 | 12 | 66 | 2.06 | 0.00 |
| SNIPER_D / D2 | 30 / 30 | -26.5 | 23 | 27 | 3.33 | 0.00 |
| SNIPER_F / F2 | 10 / 10 | +6.5 | 30 | 10 | 10.00 | 0.00 |
| SNIPER_H | 27 | -86.2 | 4 | 85 | 0 | 0 |
| SNIPER_H2 | 10 | -64.1 | 10 | 60 | 0 | 0 |
| SNIPER_BSC_FILTERED | 21 | -78.0 | 10 | 81 | 0 | 0 |

**SNIPER_B avg=-7.2% on BSC** (vs Sol's SNIPER_A baseline of ~-52%) — **45-pp better baseline on BSC**.

The dramatic A vs B divergence (avg -62.8% vs -7.2% on the same 97 tokens) is because the two streams use different trail logic. On BSC tokens, SNIPER_B's trail captures dramatically more upside.

### `paper` field on BSC
- **All 332 BSC rows have `paper=false`** (real-money trades).
- Sol: 1554 paper=true, 2921 paper=false.

The user IS running real BSC trades, currently losing on first-fire (avg -62.8% / SNIPER_A wins the dedup), while the best-fire pnl is on SNIPER_B at -7.2%. Either (a) the user's actual execution uses SNIPER_B trail (sniper architecture choice), or (b) actual execution is on SNIPER_A and the BSC P/L is significantly underwater. The brain doesn't know which without reading sniper code. **Open question for user.**

### BSC big winners (best-fire ≥150%) — 6 tokens
| time | sym | pnl% | stream | dex | liq@entry | smart | known | both | ser_only | bc_buyers | paper |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-05-18T13:00 | PORTUGAL | 906.3 | SNIPER_B | pancakeswap | 18877 | 0 | 1 | 9 | 9 | **20** | false |
| 2026-05-20T13:39 | MC | 1268.5 | SNIPER_B | pancakeswap | 16668 | 0 | 1 | 7 | 7 | **20** | false |
| 2026-05-20T14:35 | COMPUTE | 855.8 | SNIPER_B | pancakeswap | 19096 | 0 | 1 | 10 | 10 | **20** | false |
| 2026-05-20T15:55 | CATCOIN | 542.2 | SNIPER_B | pancakeswap | 24044 | 0 | 1 | 19 | 19 | **20** | false |
| 2026-05-20T16:49 | WORLDCUP | 971.2 | SNIPER_B | pancakeswap | 19052 | 0 | 1 | 10 | 10 | **20** | false |
| 2026-05-20T17:48 | CMC | 288.3 | SNIPER_F2 | pancakeswap | 28683 | 0 | 1 | 52 | 71 | **20** | false |

**Shared signature across all 6 BSC bigs**:
1. `bonding_curve_buyers.length == 20` (6/6) — at API cap.
2. `chain = bsc` (6/6) — trivially.
3. `dex = pancakeswap` (6/6) — trivially.
4. `smart = 0` (6/6) — BSC `smart` and `lp_hist`/`cr_hist`/`top1_pct`/`top5_pct` are all 0 or null. Sol-specific features don't transfer.
5. `known = 1` (6/6) — exactly 1 known wallet.
6. `both ∈ {7..19}` for 5/6 (and CMC has both=52) — substantial wallet bundle.
7. `serial_only ∈ {7..19}` for 5/6 — same wallets, similar count.
8. `liquidity_at_entry ∈ [16668, 28683]` — narrow band.
9. **5/6 bigs entered in a 4-hour window** (2026-05-20T13:39→17:48Z). PORTUGAL is the only outlier (2 days earlier).
10. `stream = SNIPER_B` (5/6) and SNIPER_F2 (1/6).

### `bonding_curve_buyers` is BSC-only
Confirmed: **0 of 4475 Sol rows** have populated `bonding_curve_buyers` (always empty array). **All 332 BSC rows** have `bonding_curve_buyers.length ∈ {0..20}` populated. Cycle_1639+'s pending investigation ("bonding_curve_buyers field is empty in our sample") was Sol-scoped — **the field exists for a different chain entirely**. The investigation is now CLOSED.

### BSC: pnl bucketed by bonding_curve_buyers count (best-fire universe)
| bc_count | n | avg | WR | rug | big% | huge% |
|---|---|---|---|---|---|---|
| 0 | 0 | (empty) | — | — | — | — |
| 1-3 | 0 | (empty) | — | — | — | — |
| 4-7 | 14 | -65.3 | 29 | 71 | 0 | 0 |
| 8-15 | 38 | -81.2 | 16 | 84 | 0 | 0 |
| **16+** | **45** | **+75.6** | **33** | **29** | **13.33** | **2.22** |

**This is the strongest gradient any cycle has produced.** bc≥16 cohort has 13.3% big rate (35× Sol best-fire baseline of 0.36%) and **+75.6% average pnl** with rug rate 29% (vs baseline 57%). Captures **6/6 bigs**.

## Step 6 — H_BSC_BC_FULL walk-forward backtest (60/20/20)

BSC universe is small (n=97 first-fire). 60/20/20 split → TRAIN=58, VAL=19, TEST=20.

**Baseline (no filter)**:
- First-fire: TRAIN -71.2 / VAL -21.6 (big=10.5%) / TEST -77.7 (big=0)
- Best-fire:  TRAIN -21.6 (big=3.5%) / VAL +103.5 (big=21.1%, huge=0) / TEST -65.6 (big=0)

**H_BC16 (bc_buyers ≥ 16)** — main candidate:
| split | first-fire (production) | best-fire (theoretical) |
|---|---|---|
| TRAIN | n=27 avg=-38.1 big=0 K=0 | n=27 avg=+41.9 big=7.4 huge=3.7 Er=+0.419 K=0.08 geom=+1.39% |
| VAL | n=9 avg=+52.3 big=22.2 Er=+0.523 K=0.58 geom=+12.2% | n=9 avg=+302.4 big=44.4 Er=+3.024 K=0.99 geom=+132.2% |
| **TEST** | **n=9 avg=-50.4 big=0 K=0** | **n=9 avg=-50.3 big=0 K=0** |

**VERDICT: FAILS walk-forward gate (Er<0 on TEST under both dedups).**

**Diagnostic — temporal clustering**: 5 of 6 BSC bigs fall in a 4-hour window on 2026-05-20T13:39→17:48Z. Under 60/20/20 by entry_time, that window lands in VAL (idx 58-76, end of VAL ≈ 17:50Z). PORTUGAL @ 13:00 (idx 0) is in TRAIN. **TEST (idx 77-96) is the post-cluster tail**, missing all bigs by construction.

This is the classic **fat-tail temporal clustering problem**: when bigs cluster in a single event-window, any time-based walk-forward splits the cluster into 1-2 partitions and the partition without the cluster looks dead.

**Composition variants tried** (`bc≥16 AND known≥1`, `bc≥16 AND liq≥15K`, `bc≥16 AND both≥7`, `stream=SNIPER_B`, etc.): all show the same TEST collapse because the 6 bigs all share the same broad shape and live in the same time window. Tightening doesn't help — the issue is data scarcity in the TEST split.

**Best best-fire variant**: `bc≥16 AND both≥7` (H_BC_LIQ_BOTH7): TRAIN n=7 avg=+291.5 huge=14.3% Er=+2.92 K=0.62 geom=+51.3%; VAL n=4 avg=+664 big=100% Er=+6.64 K=0.99 geom=+604%; **TEST n=1 — too small**.

**On first-fire (production-realistic) for the same shape**: TRAIN avg=-16.3, VAL avg=+122, TEST n=1 avg=+15.3. First-fire pnl drops the bigs from 800%+ to 5-50% range — SNIPER_A trail doesn't ride them like SNIPER_B does.

## Step 7 — what changes in our backlog / posture

### NEW: H_BSC_BC_FULL — descriptive signal, walk-forward FAILS (temporal clustering), strong observational alpha

**Filter (descriptive)**: `chain = bsc AND entry_signal.bonding_curve_buyers.length ≥ 16`.

**Evidence (best-fire dedup, BSC universe, n=97)**:
- All 6 BSC bigs satisfy filter; bc_count=20 (API cap) for all of them.
- Cohort stats: n=45 avg=+75.6% WR=33% rug=29% big=13.33% huge=2.22%.
- Walk-forward (60/20/20): TRAIN passes gate (Er=+0.419, K=0.08, geom=+1.39%); **TEST fails (Er=-0.503, K=0)** due to temporal clustering of bigs in VAL window.

**Mechanism (hypothesis)**: Pancakeswap pre-launch bonding-curve participation by ≥16 wallets indicates strong organic/insider interest *at launch*. Tokens that attract this many bonding-curve buyers tend to either pump hard (those that get listed with the bundle intact) or rug at -90%+ (bundle dumps). The cohort distribution is **bimodal** — 29% rug + 13% big = 42% of mass in the extremes vs Sol baseline ~52% rug + 0.4% big = 52% one-sided. BSC bc-full produces "real" fat tail, not just survivor-rug.

**Why not deployable as paper**:
1. Walk-forward TEST fails (Er=-0.50, K=0 under any dedup).
2. Temporal clustering — 5/6 bigs in 4-hour window means TRAIN/VAL/TEST splits are noise-dominated.
3. n=20 in TEST is at floor; even one big would flip Er positive.
4. **Production-realism gap**: BSC big-pnl is recovered by **SNIPER_B's trail**, not SNIPER_A. First-fire pnl on the 6 bigs is materially smaller (PORTUGAL 906→? need to check; cohort first-fire avg=-38 vs best-fire +41.9).

**Re-test trigger**: any new BSC big in the TEST window. With 2.4 days of BSC data, 2-3 more days should produce ~80-100 new BSC trades — enough to roll the cluster into TRAIN/VAL and have a clean TEST.

**Status**: **NEW, strong descriptive signal, NOT walk-forward-passing.** Primary monitoring target alongside H_V3. Critical to gather more BSC data before promotion.

### NEW: H_BSC_SNIPER_B_TRAIL_DIFFERENTIAL — observation

**Observation**: On 97 BSC tokens, SNIPER_A first-fire avg=-62.8%; SNIPER_B best-fire avg=-7.2%. That's a **55-pp differential** on the *same tokens*. Each of the 6 BSC bigs has the dramatic A vs B gap (e.g., PORTUGAL: A=??, B=+906).

**Implication**: SNIPER_B's trail logic on BSC is materially better than SNIPER_A's. Either (a) production should route BSC trades through B's trail logic, or (b) it already does and SNIPER_A row is just a "ghost record" that the brain misreads as the production stream. **Question raised to user — see open questions.**

**Status**: NEW (observation). Mirrors cycle_1800's SNIPER_MC_LIQ vs SNIPER_A observation on Sol — there too, "best-fire stream wins by trail logic differential". A consistent pattern across chains: **non-A streams trail bigs better; production may be leaving alpha on the table by deduping to SNIPER_A first-fire**.

### NEW: METHODOLOGY UPDATE — brain must analyze all chains, not just Solana

Previous cycles silently scoped to Sol via `!token.startsWith('0x')`. This excluded 7% of data containing the densest fat-tail cluster in the window. **Future cycles must explicitly partition by `chain` field** and run analysis per chain. Aggregating cross-chain hides the BSC-specific bonding_curve_buyers signal which doesn't exist on Sol.

**Action**: documented as future-cycle reminder in BRIEF; flagged in CRITICAL_FINDINGS.md proposal (see Step 9).

### UPDATE: H_REGIME_GUARD — both conditions RE-TRIGGERED this cycle

- Condition A (rolling-50 avg < -55%): **RE-TRIGGERED.** -43.8 → -56.6.
- Condition B (big%=0 ≥24h): **RE-TRIGGERED.** RONALDO slid out of last50 window.
- **Both active.** Cycle_1800's "both clear" was a **single-cycle head-fake** — identical pattern to cycle_1200→1328. Confirmed.

**Status**: ACTIVE via both conditions. Promotion gated.

### UPDATE: H_V3 first-fire re-test — DEPRIORITIZED

Sol first-fire bigs now n=1 (RONALDO). H_V3 walk-forward on n=1 is statistical noise. Re-queue for when 2+ Sol bigs return.

### CARRIED — unchanged this cycle

- H_SMART_CLUSTER_VETO — production-feasibility check still owed to user.
- H_TG_AS_EXIT — blocked on instrumentation.
- pumpfun_monitor / dexscreener_signals null-check — deferred.

### CLOSED — bonding_curve_buyers investigation

cycle_1639+'s pending: "bonding_curve_buyers empty in our sample". **Resolved**: it's BSC-only (always 0..20 there), always empty on Sol. The investigation surfaced this cycle's main finding. Marked CLOSED in backlog.

## Step 8 — what I considered and discarded

1. **Running H_V3 walk-forward on Sol first-fire pnl despite n=1 bigs.** Skipped — statistical noise. The result would be "no edge" by construction and waste compute.

2. **Tightening H_BC16 thresholds**. Tried `bc≥16 AND known≥1`, `bc≥16 AND both≥7`, `bc≥16 AND liq≥15K`, `bc≥16 AND stream=SNIPER_B`. All show the same TEST collapse because every big satisfies all of those conditions — adding constraints just narrows TRAIN without helping TEST. Skipped further sweeps as overfitting.

3. **Recomputing H_V3 on BSC**. H_V3 features (`smart`, `top1_pct`, `lp_unlocked`) are all 0/null on BSC trades — Sol-specific features. H_V3 is structurally Sol-only and would be vacuously empty on BSC. Skipped.

4. **Per-bigs-cluster anti-leakage check**. Confirmed: `bonding_curve_buyers` is set at `pool_created_at` (frozen at-entry), not updated. No look-ahead leakage possible. The temporal clustering is *external regime* (BSC event window) not internal leakage.

5. **Combining Sol + BSC into one universe**. Sol features like lp_hist, smart, top1_pct are null on BSC; BSC features like bonding_curve_buyers are null on Sol. The two chains are structurally different — must be analyzed separately, not unioned. Skipped.

6. **Investigating SNIPER_BSC_FILTERED stream** (n=21 BSC-only). avg=-78%, rug=81%, big=0% — clearly NOT a positive signal. Walk-forward would confirm "stay away" but it's already obvious. Low-priority.

7. **Symbol-based historical lookup of PIGEON/MTFR**. Confirmed: those tokens are mint-address-specific; current symbol-PIGEON entries are different tokens. Brain memory (HISTORY.md) is correct source for historical bigs; live state.json is rolling window. No code change needed (we never compared by symbol).

8. **Examining the 1 first-fire BSC big that's NOT in best-fire bigs**. First-fire bigs=2 vs best-fire bigs=6. 4 tokens become bigs only under best-fire (because B's trail outperforms A on those). Their identities: COMPUTE, CATCOIN, WORLDCUP, CMC at first-fire are sub-150. Implies that BSC big-capture in production depends entirely on whether the production stream routes through B's trail. Logged as part of H_BSC_SNIPER_B_TRAIL_DIFFERENTIAL.

## Step 9 — leakage catalogue check (no new entries this cycle)

1. Hindsight classifier (cycle_1702 H_RUG_PC)
2. Counting inflation (cycle_1800 H_LP_WHITELIST)
3. Time-localization artifact (cycle_1639 1AR wallet)
4. Post-entry feature (cycle_1639 ride_mode)
5. Stale classifier DB with reactive updates (cycle_0000 tokens_unified)

**Self-check on this cycle**:
- `bonding_curve_buyers` is part of `entry_signal` — frozen at entry. No leakage.
- `chain` is structural — no leakage.
- BSC walk-forward uses entry_time-based sort and 60/20/20 split — same method as prior cycles. No leakage.
- The TEST failure is **legitimate sample-size + temporal-clustering** issue, not a leakage form. The signal *appears* good but isn't yet walk-forward-validatable. Honest call: not promotable.

**New methodological note (not a leakage form per se but a related blind-spot category)**:

**Universe-scoping blind-spot** — when a research filter silently restricts to a subset (e.g., chain=solana) and the brain never asks "what's outside?", systematic alpha can hide in the excluded universe. Different from leakage (which is invalid signal); this is **missed signal**. Generic prevention: every cycle, run `partition by X for X in {chain, dex, stream, paper-vs-real}` and stat each partition. If any partition has materially different baseline, investigate before scoping further. Adding to "methodology notes" section of backlog.

## Step 10 — outputs (committed)

**Brain memory only — no sniper code changes (per cycle scope).**

- `cycle_20260521_0000.md` (this file)
- `HISTORY.md` — append 1 line
- `BRIEF.md` — rewrite (regime regressed + BSC blindspot exposed + H_BSC_BC_FULL strong descriptive)
- `backlog.md` — append H_BSC_BC_FULL, H_BSC_SNIPER_B_TRAIL_DIFFERENTIAL; close out bonding_curve_buyers pending investigation; update H_REGIME_GUARD state both-active

## Step 11 — next cycle plan

1. **Gather more BSC data**: 2-4 more cycles should produce ~60-100 more BSC trades, enough to roll the 05-20 fat-tail cluster fully into TRAIN/VAL and have a clean TEST window for H_BSC_BC_FULL re-test. If TEST shows even 1 big and Er>0, this could be the first paper-promotable signal from this brain.

2. **Run cross-chain partition systematically** at start of every cycle: `chain × stream × paper` partition table for last100 trades. Catches future universe-scoping blind-spots.

3. **Investigate BSC A vs B trail differential**: if user can share which stream actually executes in production for BSC, that determines whether the H_BSC_BC_FULL "best-fire" cohort is reachable in real money. If A is the production stream, then BSC fat tails are theoretical only and we should propose a "route BSC tokens via B-trail" paper-stream when bc≥16 fires.

4. **Sol-side**: monitor for regime stability and new bigs. H_V3 first-fire re-test stays queued. Each new Sol big increases the statistical basis.

5. **CARRIED**: H_SMART_CLUSTER_VETO production-feasibility (still owed to user); H_TG_AS_EXIT instrumentation; rugger_blacklist `wallet_added_at`.

6. **OPEN INVESTIGATION** (new): bonding_curve_buyers `bc=true` flag — every BSC big has 20 buyers with `bc=true` per row. What does `bc=true` mean (bonding-curve participant vs other)? Could there be a sub-signal in `bc=true` count vs `bc=false` count? Sample-look this cycle showed all 20 of each big had bc=true. If always true on the cap-20 set, this is no incremental signal; if sometimes mixed, that's a fresh feature.

## Step 12 — time accounting

Started 00:00. Context read ~7min, regime probe ~3min, big-token verification (PIGEON/MTFR rotation finding) ~3min, BSC discovery + profile ~5min, BSC walk-forward backtest ~5min, writeup ~15min. Total ~38min, under 45min budget.

## Key takeaways

1. **Cycle_1800's "both regime conditions clear" was a head-fake** — single-cycle local max. Both conditions re-triggered this cycle. The 05-18 collapse regime is still active 3+ days in.

2. **state.json is a rolling ~3-day window**: cycle_1800's PIGEON+3699 and MTFR+251 have already rotated out. Brain memory (HISTORY.md, insights/) is the durable record; live state.json is ephemeral.

3. **Discovered the BSC universe (332 trades = 97 unique tokens = 7% of data)** — a major blind-spot carried since cycle_1639. Brain analyses had silently scoped to Solana via `!token.startsWith('0x')` filter and never probed what was excluded.

4. **`bonding_curve_buyers` field is BSC-only** (0/4475 populated on Sol, 332/332 on BSC). Cycle_1639+'s pending investigation resolved.

5. **H_BSC_BC_FULL (`bc_buyers ≥ 16`) is the strongest descriptive cohort signal any cycle has produced**: n=45 best-fire avg=+75.6% big=13.3% huge=2.2% (vs Sol best-fire baseline big=0.36%). Catches 6/6 BSC bigs (PORTUGAL +906, MC +1268, COMPUTE +856, CATCOIN +542, WORLDCUP +971, CMC +288).

6. **H_BSC_BC_FULL walk-forward FAILS on TEST** — 5/6 BSC bigs cluster in a 4-hour event window on 2026-05-20, landing in VAL split. TEST window is post-cluster and barren. Classic fat-tail temporal-clustering problem. Need more BSC data to re-evaluate.

7. **BSC trades are REAL MONEY** (`paper=false` on all 332 rows). User has live BSC sniping with avg first-fire pnl -62.8% (SNIPER_A) but best-fire pnl -7.2% (SNIPER_B). 55-pp differential on the same 97 tokens. The 6 BSC bigs all win best-fire on SNIPER_B's trail; SNIPER_A first-fire catches only 2 bigs. Mirrors Sol cycle_1800's MC_LIQ vs A finding.

8. **H_V3 first-fire re-test (planned task) deprioritized**: PIGEON+MTFR rotation reduced Sol bigs to n=1 (RONALDO). Walk-forward on n=1 is not informative. Re-queue when 2+ Sol bigs return.

9. **New methodology**: every cycle should partition by `chain × stream × paper` before scoping deeper analysis. Universe-scoping blind-spots are a distinct failure mode from leakage — invalid scoping ≠ invalid signal.

10. **Strategic outlook**: BSC + bonding_curve_buyers is the most promising track this brain has produced for +1M%. **High-conviction next step**: wait 2-4 cycles for clean TEST window, re-run H_BSC_BC_FULL. If validated, BSC bc-full could become the first paper-stream candidate.
